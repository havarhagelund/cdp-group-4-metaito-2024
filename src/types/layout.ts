export type size = {
  width: number;
  height: number;
};

export type position = {
  x: number;
  y: number;
};

export type index2D = {
  index: number;
  row: number;
};

export type direction = "bottom" | "right" | null;
